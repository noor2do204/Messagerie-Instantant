<div class="navbar navbar-expand-lg navbar-dark bg-dark shadow" style="height: 80px;">
    <div class="container d-flex justify-content-between align-items-center">
        <a class="navbar-brand text-primary" href="{{ path('app_home') }}" style="font-size: 28px;">
            ChatTech
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link text-white mx-2" href="{{ path('app_conversation_index') }}" style="font-size: 18px;">Discussions</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white mx-2" href="{{ path('friends') }}" style="font-size: 18px;">Gestion des amis</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link bg-danger text-white mx-2" href="{{ path('app_logout') }}" style="font-size: 18px;">
                            Se Déconnecter
                        </a>
                    </li>


                {% else %}
                    <li class="nav-item">
                        <a class="nav-link text-white mx-2" href="{{ path('app_register') }}" style="font-size: 18px;">S'inscrire</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white mx-2" href="{{ path('app_login') }}" style="font-size: 18px;">Se Connecter</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</div>
