{% extends 'base.html.twig' %}

{% block title %}Edit Conversation{% endblock %}

{% block body %}
    <div class="container mt-5">
    <div class="row justify-content-center mb-5">
        <div class="col text-center">
            <h1 class="text-white">Modifier une discussion</h1>
        </div>
    </div>

    {{ include('conversation/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_conversation_index') }}" class="btn btn-primary mt-3">Retour</a>

    {{ include('conversation/_delete_form.html.twig') }}
{% endblock %}
